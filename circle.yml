version: 2
jobs:
  build:
    machine:
      image: docker-17.06.0-ce

    working_directory: /tmp

    steps:
    - run: uname -a
    - run : docker info
    - run : docker version
    - run: cat /etc/os-release
