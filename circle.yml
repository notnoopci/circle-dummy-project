version: 2
jobs:
  build:
    machine:
      image: circleci/classic:201708-01

    working_directory: /tmp

    steps:
    - run: sudo apt-get update
    - run: uname -a
    - run : docker info
    - run : docker version
    - run: cat /etc/os-release
