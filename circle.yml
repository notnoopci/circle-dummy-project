version: 2
jobs:
  build:
    machine: true
    working_directory: /tmp

    steps:
    - run: uname -a
    - run : docker info
    - run : docker version
    - run: cat /etc/os-release
    - run: docker-compose --version
