# empty
dependencies:
  override:
  - sudo apt-get update; sudo apt-get install -y eatmydata sysbench

test:
  override:
  - ./sysbench-cpu.sh 2>&1 | tee $CIRCLE_ARTIFACTS/cpu.txt
  - mkdir -p io && cd io && ~/circle-dummy-project/sysbench-lio.sh 2>&1 | tee $CIRCLE_ARTIFACTS/io-localdisk.txt
  - mkdir -p emd && cd emd && eatmydata ~/circle-dummy-project/sysbench-lio.sh 2>&1 | tee $CIRCLE_ARTIFACTS/io-localdisk-eatmydata.txt
  - mkdir -p /dev/shm/sysbench && cd /dev/shm/sysbench && SIZE=3 ~/circle-dummy-project/sysbench-lio.sh 2>&1 | tee $CIRCLE_ARTIFACTS/io-shm.txt
  - mysql -e 'create database sbtest;'
  - ./sysbench-mysql.sh 2>&1 | tee $CIRCLE_ARTIFACTS/mysql.txt
