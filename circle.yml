# empty
machine:
  post:
  - pwd
dependencies:
  pre:
  - pwd
  - echo config pre dependencies
  override:
  - echo config override dependencies
  post:
  - echo config post dependencies
test:
  override:
   - echo from config
  post:
  - echo config post test
  - pwd
