version: 2
jobs:
  build:
    machine:
      image: circleci/classic:201707-02
    working_directory: /tmp

    steps:
    - run: uname -a
    - run : docker info
    - run : docker version
    - run: cat /etc/os-release
