version: 2
build_agent: picard:0.0.0-8266562
jobs:
  build:
    machine: true
    working_directory: /tmp

    steps:
    - run: uname -a
    - run : docker info
    - run : docker version
    - run: cat /etc/os-release
